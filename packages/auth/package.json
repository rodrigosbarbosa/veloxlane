{
  "name": "@veloxlane/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Shared auth utilities for VeloxLane",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@veloxlane/schemas": "workspace:*"
  },
  "devDependencies": {
    "@veloxlane/config": "workspace:*",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
