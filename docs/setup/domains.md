# Domains and DNS — VeloxLane

Required:

- `veloxlane.com`

Recommended defensive domains:

- `veloxlane.co`
- `veloxlane.io`

Recommended registrar/DNS:

- Cloudflare Registrar + Cloudflare DNS, or Namecheap registrar with Cloudflare DNS.

Founder actions:

1. Register `veloxlane.com`.
2. Optionally register `.co` and `.io`.
3. Enable registrar lock.
4. Enable MFA on registrar and DNS account.
5. If using Cloudflare, add the domain to Cloudflare DNS.

Return to Hermes:

- Registrar name.
- Domains registered.
- DNS provider.
- No passwords/API tokens yet.
